package fr.ubordeaux.scrabble.model.enums;

/**
 * Represents the direction of a word on the board.
 */
public enum Direction {
  /** Word placed horizontally (left to right). */
  HORIZONTAL,
  /** Word placed vertically (top to bottom). */
  VERTICAL
}
